<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        The Janam XR2/XT30 UHF module is reached through the device's serial port via
        URFIDLibrary, which needs no dedicated runtime permission (unlike Chainway's
        com.rscja.permission.UHF). The platform_sdk classes (android.device.*) are
        supplied by the device firmware.
    -->

    <application
        android:label="StoreLense X22"
        tools:replace="android:label">
    </application>

</manifest>
