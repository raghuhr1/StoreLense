server:
  port: 8090

spring:
  application:
    name: erp-integration-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:storelense}
    username: ${DB_USERNAME:storelense_app}
    password: ${DB_PASSWORD:changeme}
    hikari:
      schema: erp
      maximum-pool-size: 5
      minimum-idle: 1
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: erp
        jdbc.batch_size: 500
        order_inserts: true
        order_updates: true
    open-in-view: false
  flyway:
    enabled: true
    schemas: erp
    locations: classpath:db/migration
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      properties:
        enable.idempotence: true
        spring.json.add.type.headers: "true"
    consumer:
      group-id: erp-integration-service
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "com.storelense.*"

storelense:
  erp:
    base-url: ${ERP_BASE_URL:http://erp.internal/api}
    api-key: ${ERP_API_KEY:}
    api-version: ${ERP_API_VERSION:v1}
    connect-timeout: 10s
    read-timeout: 30s
    page-size: 500
    product-sync-interval: PT6H
    inventory-sync-cron: "0 0 1 * * *"
    max-retries: 3
    push-soh-enabled: ${ERP_PUSH_SOH_ENABLED:true}
    import:
      store-id: ${ERP_IMPORT_STORE_ID:}
      local-enabled: ${ERP_IMPORT_LOCAL_ENABLED:false}
      local-folder: ${ERP_IMPORT_LOCAL_FOLDER:/tmp/erp-import}
      s3-enabled: ${ERP_IMPORT_S3_ENABLED:false}
      s3-bucket: ${ERP_IMPORT_S3_BUCKET:}
      s3-prefix: ${ERP_IMPORT_S3_PREFIX:erp/import/}
      s3-region: ${ERP_IMPORT_S3_REGION:us-east-1}
      s3-poll-delay-ms: ${ERP_IMPORT_S3_POLL_MS:60000}
  product-service:
    base-url: ${PRODUCT_SERVICE_URL:http://product-service:8082}
    service-token: ${PRODUCT_SERVICE_TOKEN:}
  soh-service:
    base-url: ${SOH_SERVICE_URL:http://soh-service:8085}
    service-token: ${SOH_SERVICE_TOKEN:}
  jwt:
    secret: ${JWT_SECRET:storelense-dev-secret-key-minimum-32-chars}
    access-token-expiry-ms: 900000
    refresh-token-expiry-ms: 604800000

resilience4j:
  retry:
    instances:
      erp:
        max-attempts: 3
        wait-duration: 2s
        retry-exceptions:
          - org.springframework.web.client.ResourceAccessException
          - org.springframework.web.client.HttpServerErrorException
  circuitbreaker:
    instances:
      erp:
        failure-rate-threshold: 50
        wait-duration-in-open-state: 2m
        sliding-window-size: 10
        minimum-number-of-calls: 5

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    tags:
      service: erp-integration-service

logging:
  level:
    com.storelense.erp: INFO
