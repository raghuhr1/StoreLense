# ─── StoreLense Observability stack — copy to .env.observability ─────────────
# These are merged with .env when starting the obs stack

# Grafana admin password
GRAFANA_PASSWORD=StoreLense@Obs1

# Email alerts (optional — leave GRAFANA_SMTP_ENABLED=false to disable)
GRAFANA_SMTP_ENABLED=false
GRAFANA_SMTP_HOST=smtp.gmail.com:587
GRAFANA_SMTP_USER=your-email@gmail.com
GRAFANA_SMTP_PASSWORD=your-app-password
GRAFANA_SMTP_FROM=alerts@storelense.in
