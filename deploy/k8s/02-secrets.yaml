# IMPORTANT: Replace base64 values before deploying to any real environment.
# Generate with: echo -n 'value' | base64
# In production use ExternalSecrets / Vault instead of plain Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: storelense-secrets
  namespace: storelense
type: Opaque
data:
  # echo -n 'changeme' | base64
  DB_PASSWORD:    Y2hhbmdlbWU=
  # echo -n 'storelense-dev-secret-key-minimum-32-chars' | base64
  JWT_SECRET:     c3RvcmVsZW5zZS1kZXYtc2VjcmV0LWtleS1taW5pbXVtLTMyLWNoYXJz
  # echo -n '' | base64
  REDIS_PASSWORD: ""
  # echo -n 'dev-key' | base64
  ERP_API_KEY:    ZGV2LWtleQ==
